# v1.4.1「運用品質パッチ」設計書

> **ステータス：実装完了（v1.4.1）**  
> 最終更新：2026-06-28  
> v1.4.0 リリース直後の運用摩擦解消。機能追加ではなく案内・安全性強化。

---

## 0. 目的

v1.4.0 で TEXT チェーン接続後も、開発者が以下で迷いやすい:

- dry-run なのに `latest` が更新される
- `output/` が `git status` に残る（追跡ファイル）
- `--apply` 前の API キー判断
- 陳腐化した Next Actions / CLI help

v1.3.1 と同様、**運用品質パッチ** として report / README / CLI を強化する。

---

## 1. スコープ

### 実装する

| # | 内容 |
|---|------|
| 1 | 陳腐化文案修正（MANUAL_REVIEW_ONLY、OpenAI ヒント、CLI help） |
| 2 | report.md 3 セクション追加 |
| 3 | `collectUsedTools` + API キー案内精度 |
| 4 | CLI apply バナー + Summary Next Actions |
| 5 | README 運用追記 |
| 6 | docs / テスト |

### 実装しない

- Smart Auto Fix / Regeneration Engine 中核
- `quality_pipeline.js` ループ構造
- `run_daily.sh` 変更
- `openai_regenerate` / `--resume` / GitHub Actions
- `output/` git rm --cached

---

## 2. report.md 追加セクション

1. **通常 commit 不要の副産物** — reports vs output、整理コマンド
2. **dry-run / latest / archive** — dry-run でも latest 更新、退避タイミング
3. **--apply 実行判断** — キーあり/なし、rootCause 別必要処理

---

## 3. テスト（Phase 4-E 相当）

- report.md セクション存在
- 陳腐化文案なし
- smart_auto_fix → nano_banana + gemini ヒント
- CLI help 更新
- `npm run test:quality-pipeline` PASS
- `run_daily.sh` diff 空

---

*v1.4.1 設計書 — 運用品質パッチ*
