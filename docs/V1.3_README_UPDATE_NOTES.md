# v1.3 README / CHANGELOG / VERSION 更新前メモ

> Phase 7 時点の仕上げ前チェックリスト。**Phase 8 で README / CHANGELOG / VERSION を更新済み。**

---

## npm scripts（追加済み）

| script | 用途 |
|--------|------|
| `npm run quality-pipeline` | デフォルト（dry-run）で品質パイプライン実行 |
| `npm run quality-pipeline:dry-run` | 明示 dry-run |
| `npm run quality-pipeline:apply` | API 実行モード |
| `npm run quality-pipeline:report` | REPORT フェーズから実行 |
| `npm run quality-pipeline:export` | EXPORT フェーズから実行 |
| `npm run test:quality-pipeline` | 最小テスト（API 未使用） |

追加引数は `--` の後に渡す:

```bash
npm run quality-pipeline:dry-run -- --from-phase image-review --max-rounds 3
npm run quality-pipeline:apply -- --from-phase image-review --allow-partial-export
```

---

## run_daily との使い分け（README に記載推奨）

| | `npm run daily` | `npm run quality-pipeline` |
|--|-----------------|----------------------------|
| 位置づけ | v1.0〜v1.1 従来一括 | v1.3 品質ループ付き上位パイプライン |
| 画像改善 | OpenAI 再生成中心 | rootCause 別（Nano Banana 等） |
| 合格基準 | 80 点で export | 90 点まで自動ループ |
| 変更 | **維持（変更しない）** | 新規追加 |

---

## 実装済み（MVP v1.3.0）

- Phase 1〜2: 基盤 + オーケストレータ骨格
- Phase 3: HEALTH_CHECK / IMAGE_REVIEW 接続
- Phase 4: 改善ループ（Nano Banana 実改善、stopReason 分割）
- Phase 5: export（improved 画像採用、`allow-partial-export`）
- Phase 6: report（`quality_pipeline_report`）
- Phase 7: npm scripts + `test:quality-pipeline`

## 未実装（README に「将来」と明記推奨）

- POST_GENERATION 〜 IMAGE_GENERATION の実接続（skipContent デフォルト外）
- smart_auto_fix / openai_regenerate の実改善
- `--resume` 途中再開
- 実行履歴スナップショット（`reports/quality-pipeline/YYYY-MM-DD-HHmmss/`）
- Hook 通知（Slack 等）
- v1.2 CLI の subprocess 完全置換

---

## テスト手順（公開前確認）

```bash
npm run test:quality-pipeline
npm run quality-pipeline:dry-run -- --from-phase image-review --max-rounds 3
# fixture あり・API キーありの環境のみ:
# npm run quality-pipeline:apply -- --from-phase image-review --max-rounds 1
```

期待出力（latest）:

- `reports/quality-pipeline/latest/pipeline_state.json`
- `reports/quality-pipeline/latest/metrics.json`
- `reports/quality-pipeline/latest/report.json`
- `reports/quality-pipeline/latest/report.md`
- apply + export 時: `output/instagram/`

---

## CHANGELOG 追記案（v1.3.0）

- 品質パイプライン CLI（`scripts/run_quality_pipeline.js`）
- 改善ループ（IMPROVEMENT ⇄ RE_REVIEW、maxRounds）
- Nano Banana 実改善接続
- improved 画像の Instagram Package 採用
- REPORT_SCHEMA 準拠 `quality_pipeline_report`
- 終了コード統合（`getPipelineExitCode`）

---

## VERSION.md 追記案

- v1.3.0: 完全自動品質パイプライン MVP（dry-run 標準、`--apply` で本番）
